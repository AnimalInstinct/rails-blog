<div class="col-sm-12 col-md-6 col-lg-4">
  <div class="card p-4">
    <div class="card-body">
      <h2 class="card-title mb-4"><%= article.title %></h2>
      <p class="card-text mb-4"><%= truncate(article.description, :length => 100) %></p>
      <%= link_to 'Rread tha...', article_path(article), class: "btn btn-outline-secondary btn-lg" %>
    </div>
  </div>
</div>